name: minisoccer-microservice-app
services:
  payment-service:
    container_name: payment-service
    # image: terenjit/user-service:1
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8003:8003" 
    env_file:
      - .env
    volumes:
      - ./config.json:/app/config.json
    